apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.0"

    defaultConfig {
        minSdkVersion 18
        targetSdkVersion 25
        versionCode 1
        versionName "2017.02.27"
    }
}

dependencies {
    compile 'com.android.support:support-annotations:25.0.0'
}
